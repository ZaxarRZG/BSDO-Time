/*
let one = prompt("число1", 0);
let two = prompt("число2", 0);
alert(`число ${one + two}`);


let age = confirm("тебе есть 18?");
    if (age == true){
         alert ("проходи");
    }
    else location.reload();


находим дату
const now = new Date();

const year = now.getFullYear();   // Полный год (например, 2023)
const month = now.getMonth();     // Месяц (от 0 до 11! Январь это 0)
const day = now.getDate();        // День месяца (от 1 до 31)
const hours = now.getHours();     // Часы (от 0 до 23)
const minutes = now.getMinutes(); // Минуты

 сравнение года
let YEAR = prompt('какой сейчас год?(xxxx)');
if (YEAR < year){
    alert('маловато');
    let YEAR = prompt('ещё раз какой год?');
}
else if (YEAR > year){
    alert('многовато');
    let YEAR = prompt('ещё раз какой год?');
}
else {
    alert('првильно');
}

сравнение месяца
let MONTH = prompt('какой сейчас месяц?');

if (MONTH < 3){
    alert('маловато');
    let MONTH = prompt('ещё раз какой месяц?');
}
else if (MONTH > 3){
    alert('многовато');
    let MONTH = prompt('ещё раз какой месяц?');
}
else {
    alert('првильно');
}

сравнение дня
let DAY = prompt('какой сейчас день?');
if (DAY < 4){
    alert('маловато');
    let DAY = prompt('ещё раз какой день?');
}
else if (DAY > 4){
    alert('многовато');
    let DAY = prompt('ещё раз какой день?');
}
else {
    alert('првильно');
}
*/

/*
let age = prompt('сколько тебе лет 2?')
let accessAllowed = (age > 18) ? true : false;
*/

// const objectA = {
//     a: function logandconsole() {
//         for (let i = 0; i <= 10; i++){
//             console.log(i);
// }
// }
// }

// objectA.a();
// object.A() 



let chaina = confirm("пройдёте опрос?")
if (chaina == true){

let value = prompt('как будет <выгодный> на китайском', '');

    if (value == 'hui') {
        alert('Верно!');
    } else {
        alert('не каждый знает ƪ(˘⌣˘)ʃ'); 
    }
}
else location.reload();